# Lane C — Discovery / shared integration evidence

Owned flows: `2–20, 32–59, 95–97`, plus shared-file integration and checklist acceptance.

Current queue: **2–6/42** → 7–13 → 14–20/32/37–41/96–97 → 33–36/38–39 → 43–51 → 52–59 → 95.

## Evidence blocks

Append; do not rewrite prior blocks.

No family has been accepted under the new evidence ledger yet. Existing browser comparisons, routes and historical test passes are useful evidence candidates but must be reproduced/anchored to the current commit before checking `Accept`.

Lane C also reviews Lane A/B evidence, applies shared-file requests, runs quiet-window integration gates, updates `shop-parity-checklist.md`, and is the only lane permitted to push `main` while parallel mode is active.